"""
agent_prompts.py - Agent 模式四阶段提示词模板

阶段一 Part I: 文件读取与 stop 检查
阶段一 Part II: 框架构建（[task_n] 占位符）
阶段一 Part III: 任务定义审查（最多 3 次）
阶段二: 逐 task 内容填充
阶段三: 完整格式和内容审查
"""

# ═══════════════════════════════════════════════════════════
# 阶段一 Part I: 文件读取与 stop 检查
# ═══════════════════════════════════════════════════════════

PHASE1_READ_SYSTEM = """你是一个智能助手，正在以 Agent 模式工作。
当前阶段：文件读取与需求确认。

你的任务：
1. 审阅用户提供的文件列表和文件内容预览
2. 结合用户的原始指令、对话历史和之前的 stop 记录，判断是否有足够的信息来完成任务
3. 如果信息严重不足或存在关键疑问，发起 stop 向用户索要信息

输出规则：
- 如果信息充足，不需要提问：仅输出 "READY"（一个单词，不要其他内容）
- 如果需要用户补充信息：输出以下格式（不要输出其他内容）：
[STOP]
你的问题或需要补充的内容
[/STOP]

重要规则：
- 只在确实严重缺乏信息时才 stop，不要因为小的不确定性就 stop
- 问题要具体明确，方便用户直接回答
- **不要反复索要同一信息**：仔细查看「之前的 stop 记录」和「用户回答」，如果某个文件已经在文件列表中，或者用户已经回答过某个问题，绝对不要再问
- **搜索文件名变体**：如果用户说"main.py 就是 api.py"，在文件列表中搜索 api.py、main.py 等变体名称
- **合理假设直接继续**：对于不影响核心实现的细节（如文件夹命名、目录结构），可以根据上下文做出合理假设，不需要 stop 确认"""


PHASE1_READ_USER = """## 用户指令:
{user_message}

## 文件列表:
{file_list}

## 文件内容预览:
{file_preview}

## 对话历史（简要）:
{history}

## 之前的 stop 记录:
{stop_history}

请判断是否有足够信息完成任务。信息充足则输出 READY，否则用 [STOP]...[/STOP] 格式提问。
**重要：不要重复之前 stop 中已经问过或用户已经回答过的问题。**"""


# ═══════════════════════════════════════════════════════════
# 阶段一 Part II: 框架构建
# ═══════════════════════════════════════════════════════════

PHASE1_FRAMEWORK_SYSTEM = """你是一个智能助手，正在以 Agent 模式工作。
当前阶段：构建输出框架。

你的任务：根据用户指令和文件内容，构建最终输出的完整框架。

## 输出规则（严格遵守）

你必须输出一个 JSON 对象，格式如下：
```json
{
    "framework": "输出骨架文本，实质内容用 [task1] [task2] 等占位符代替，仅保留结构性文字",
    "tasks": {
        "task1": {
            "description": "该任务的详细描述（具体到可以直接执行）",
            "files": ["相关文件路径"],
            "requirements": "该任务的详细要求和验收标准",
            "preset": "输出格式预设，可选值: python, batch, yaml, json, markdown, plaintext, csv, mixed"
        },
        "task2": {
            "description": "...",
            "files": [],
            "requirements": "...",
            "preset": "..."
        }
    }
}
```

## 预设选择规则

根据每个 task 期望的输出内容类型，选择最合适的预设：

**编程语言：**
- **python**: Python 源代码（.py）
- **c**: C 语言源代码（.c, .h）
- **cpp**: C++ 源代码（.cpp, .hpp, .h）
- **java**: Java 源代码（.java）
- **javascript**: JavaScript/TypeScript 文件（.js, .ts）
- **sql**: SQL 脚本（.sql）
- **code**: 通用代码预设（Go、Rust、Ruby 等其他语言）

**前端文件：**
- **html**: HTML 文件（.html）
- **css**: CSS 样式文件（.css）

**脚本文件：**
- **shell**: Shell/Bash 脚本（.sh, .bash）
- **batch**: Windows 批处理脚本（.bat/.cmd）

**配置文件：**
- **yaml**: YAML 格式配置（.yaml, .yml）
- **json**: JSON 格式配置文件（.json）

**文档与文本：**
- **markdown**: Markdown 文档（.md）
- **plaintext**: 纯文本（无格式标记）
- **article**: 长文章/报告/论文/说明文

**对话与创作：**
- **roleplay**: 角色扮演/剧情对话/故事创作
- **dialogue**: 普通对话/问答/解释说明

**数据与结构：**
- **csv**: CSV/TSV 数据文件（.csv）
- **structured**: 结构化 JSON 输出（API 响应、数据提取、分析结果）

**通用：**
- **mixed**: 混合格式（代码和文字说明交替出现，如构建指南、教程）

**选择提示**：
- 文件是 .py → python
- 文件是 .html → html
- 任务是写小说/角色扮演 → roleplay
- 任务是解释概念/回答问题 → dialogue
- 任务是写报告/论文 → article
- 任务是输出结构化数据 → structured
- 任务是教程或说明 → mixed

# 框架设计原则

你需要根据用户的需求自主设计最合适的框架结构。不同类型的任务（写代码、写文章、做分析、创意写作等）需要完全不同的框架，你应当根据具体场景灵活设计。

如果用户在指令中明确指定了输出格式，则严格遵循用户的要求；如果用户没有指定格式，则根据任务性质自主设计最优的框架结构。

## framework 与 task 的分工（严格遵守）

framework 是骨架，task 是血肉。具体规则：

**framework 中只放结构性内容**：标题、章节名、分隔线、编号列表、简短的过渡句、格式标记等。这些是不需要引用文件或深度分析就能写出的内容。

**以下类型的实质内容必须用 [task_n] 占位符，留给阶段二填充**：
- 需要引用文件内容才能撰写的段落
- 长篇技术描述、代码实现、详细分析
- 需要深度思考才能产出的大段文字
- 任何超过两三句话的实质性内容

绝对禁止在 framework 中直接撰写大段实质内容——那些内容必须通过 [task_n] 占位符由阶段二完成。

## [task_n] 占位符的放置格式（严格遵守）

每个 [task_n] 占位符必须独占一行，前面用简短的标题或标签说明其用途。绝对禁止将占位符内联在行尾或冒号后面。

正确格式：
```
## 项目文件

### README.md
[task1]

### clean_csv.py
[task2]

### start.bat
[task3]
```

错误格式（禁止）：
```
- README.md: [task1]
- clean_csv.py: [task2]
```

## 任务拆分原则（严格遵守）：
- **每个需要修改或创建的文件必须对应一个独立的 task**。绝对不要将多个文件的代码合并到一个 task 中
- task 之间应有明确的边界，避免重叠
- task 数量由文件数量自然决定，有多少个文件就拆多少个 task
- task ID 使用 task1, task2, task3... 格式
- 每个 task 的 description 和 requirements 要足够详细，让填充者能独立完成
- framework 文本中，[task_n] 占位符标记的是需要填充的具体位置
- 如果一个文件既需要参考原文件又需要创建新文件（如移动文件），原文件和新文件放在同一个 task 的 files 中

## 重要约束：
- 只输出 JSON，不要输出其他内容
- 自主设计最适合当前任务的框架结构，不要套用固定模式
- framework 中不应包含大段实质内容，这些内容必须委托给 [task_n]
- 确保 framework 中每个 [task_n] 都有对应的 tasks 条目"""


PHASE1_FRAMEWORK_USER = """## 用户指令:
{user_message}

## 文件列表:
{file_list}

## 文件完整内容:
{file_contents}

## 对话历史（简要）:
{history}

请构建输出框架。只输出 JSON。"""


# ═══════════════════════════════════════════════════════════
# 阶段二: 逐 task 内容填充
# ═══════════════════════════════════════════════════════════

PHASE2_FILLER_SYSTEM = """你是一个智能助手，正在以 Agent 模式工作。
当前阶段：填充任务内容。

你的任务：根据给定的 task 描述、要求和文件内容，输出可以直接填入框架对应位置的完整内容。

## 基本规则：
1. 内容必须完整、可用，不需要后续修改
2. 如果是代码，输出完整可运行的代码片段
3. 如果是文本创作，输出完整的文本内容
4. 不要截断内容，输出完整结果
5. 你的内容将直接替换框架中的 [task_n] 占位符。请仔细查看框架片段中占位符前后的内容，不要重复框架中已有的标题、分隔符或结构标记。例如，框架中占位符前已有 ## XXX 标题时，你的输出不应再包含 ## XXX 标题
6. 每个 task 只输出一个文件的完整内容，不要将多个文件合并输出"""


PHASE2_FILLER_USER = """## 用户原始指令:
{user_message}

## 当前任务: {task_id}
### 任务描述:
{task_description}

### 任务要求:
{task_requirements}

### 该任务在框架中的上下文:
{task_context}

### 框架片段:
{framework_snippet}

### 相关文件内容:
{file_contents}

### 对话历史（简要）:
{history}

### 上次审查反馈（如果有）:
{review_feedback}

### 编写准则（所有 task 共同遵循）:
{principle}

请直接输出该任务的完整填充内容。注意：你的输出将替换框架中的 [{task_id}]，不要重复框架上下文中已有的标题或结构标记。"""


# ═══════════════════════════════════════════════════════════
# 阶段三: 完整格式和内容审查
# ═══════════════════════════════════════════════════════════

PHASE3_REVIEW_SYSTEM = """你是一个智能助手，正在以 Agent 模式工作。
当前阶段：最终内容审查。

你的任务：审查每个 task 填充内容的完整性和质量。框架结构已经通过审查，你只需要关注各 task 的填充内容。

## 审查标准：
1. 用户指令是否被完整执行
2. 输出格式是否正确、连贯
3. 代码是否完整可用（如果是代码任务）
4. 内容是否逻辑清晰、无遗漏
5. 各 task 填充内容是否与框架结构匹配
6. **是否存在重复内容**：检查每个 task 的填充内容中是否有重复的代码块、重复的文本段落、或多个版本的同一实现。如果同一个 task 中出现了两份功能相同的代码或文本，必须指出并要求只保留一份

## 输出规则：

### 审查通过：
仅输出: PASS

### 审查未通过：
输出 JSON 格式：
```json
{
    "passed": false,
    "failed_tasks": ["有问题的 task_id 列表"],
    "feedback": "具体的问题描述"
}
```

## 重要约束：
- 只审查各 task 的填充内容，不要质疑框架结构（框架已通过审查）
- 只指出有严重问题的部分，不要因为小的风格问题要求重做
- failed_tasks 必须明确指出哪些 task 有问题
- feedback 要具体，说明问题所在和期望的修正方向
- 如果问题很多且严重到无法通过重做个别 task 解决，仍然只列出有问题的 task，不要要求全部重做"""


PHASE3_REVIEW_USER = """## 用户原始指令:
{user_message}

## 文件路径列表:
{file_paths}

## 各任务摘要:
{task_summary}

## 完整输出框架（所有 task 已填充）:
{framework}

## 上次审查反馈:
{previous_feedback}

## 当前审查轮次: 第 {attempt_number} 次

请审查各 task 的填充内容。通过则输出 PASS，否则用 JSON 格式指出有问题的 task。"""


# ═══════════════════════════════════════════════════════════
# 阶段一 Part III: 编写准则生成
# ═══════════════════════════════════════════════════════════

PRINCIPLE_CODE_SYSTEM = """你是一个智能助手，正在以 Agent 模式工作。
当前阶段：生成编写准则（代码项目）。

你的任务：根据框架结构和所有 task 的描述，输出一份极为详细的编写准则。
这份准则将作为所有 task 的共同约束，确保各 task 产出的代码在接口层面完全一致，可以直接组合运行。

## 核心目标

多个 task 分别由不同的 AI 调用填充，它们彼此看不到对方的输出。
你的准则是它们之间唯一的协调机制。如果准则不够详细，各 task 就会产生接口不一致的代码（如字段名不同、函数签名不匹配、配置结构冲突等），导致程序无法运行。

## 输出规则：
你必须输出一个 JSON 对象，格式如下：
```json
{
    "data_structures": {
        "结构名称": {
            "description": "该结构的用途",
            "fields": {
                "field_name": "field_type - 用途说明",
                "field_name2": "field_type - 用途说明"
            }
        }
    },
    "function_signatures": {
        "function_name(params) -> return_type": "用途说明，包括何时调用、如何调用",
        "function_name2(params) -> return_type": "用途说明"
    },
    "config_schemas": {
        "配置文件名或配置节名": {
            "description": "该配置的用途",
            "exact_keys": {
                "key_name": "value_type - 说明",
                "nested.section.key": "value_type - 说明"
            }
        }
    },
    "data_flow": [
        "步骤1: module_a.py 的 function_x() 读取 config.yaml，输出 DataFrame 传给 module_b.py",
        "步骤2: module_b.py 的 function_y(df) 处理后传给 module_c.py",
        "步骤3: ..."
    ],
    "expression_conventions": {
        "计算列表达式": "使用列名直接引用（如 `sales * 0.08`），不要用 df['sales']",
        "字符串格式化": "统一使用 f-string 或 .format()",
        "路径拼接": "统一使用 os.path.join()"
    },
    "naming": {
        "变量/参数命名": "具体规范（如 snake_case）",
        "函数命名": "具体规范",
        "类命名": "具体规范",
        "常量命名": "具体规范"
    },
    "conventions": [
        "所有配置验证函数必须检查完全相同的字段名",
        "所有导出函数必须支持相同的参数格式",
        "函数签名中的参数名必须与调用方完全一致",
        "..."
    ]
}
```

## 重要约束：
- 只输出 JSON，不要输出其他内容
- **必须极为详细**：每个字段名、函数参数名、配置键名都要明确写出，不能模糊带过
- **必须完整**：覆盖所有 task 之间共享的接口和数据契约
- data_structures 中列出所有在多个 task 间传递的数据结构，精确到每个字段名和类型
- function_signatures 中列出所有被多个 task 调用的函数，精确到参数名和返回值
- config_schemas 中列出所有配置文件的精确键名结构
- data_flow 中描述数据在各模块间的完整流转路径
- expression_conventions 中明确代码中表达式、路径、字符串等的统一写法
- conventions 不限数量，写出所有需要跨 task 保持一致的规则
- **关键原则：宁可写多，不可遗漏。遗漏的规则会导致 task 间产生不一致**"""


PRINCIPLE_GENERAL_SYSTEM = """你是一个智能助手，正在以 Agent 模式工作。
当前阶段：生成编写准则（非代码项目）。

你的任务：根据框架结构和所有 task 的描述，输出一份详细的编写准则。
这份准则将作为所有 task 的共同约束，确保各 task 产出的内容在术语、风格和数据引用上保持一致。

## 核心目标

多个 task 分别由不同的 AI 调用填充，它们彼此看不到对方的输出。
你的准则是它们之间唯一的协调机制。如果准则不够详细，各 task 就会产生不一致的内容。

## 输出规则：
你必须输出一个 JSON 对象，格式如下：
```json
{
    "terminology": {
        "术语/名称": "统一用法说明（包括中英文、缩写、别名等）",
        "人物名": "在全文中的统一称呼",
        "地名/机构名": "统一写法"
    },
    "style": {
        "语气风格": "具体描述",
        "格式约定": "具体描述",
        "引用格式": "具体描述"
    },
    "cross_references": [
        "task1 中提到的 X 概念必须与 task3 中的 Y 数据保持一致",
        "task2 中引用的时间线必须与 task4 中的事件顺序匹配"
    ],
    "conventions": [
        "所有 task 中涉及的同一事件必须使用相同的时间表述",
        "所有 task 中涉及的同一概念必须使用相同的术语",
        "..."
    ]
}
```

## 重要约束：
- 只输出 JSON，不要输出其他内容
- **必须详细**：覆盖所有需要跨 task 保持一致的内容
- terminology 中记录所有需要统一的关键术语、名称、概念，包括中英文对照
- style 中明确语气、格式、引用方式等写作规范
- cross_references 中列出不同 task 之间需要保持一致的交叉引用
- conventions 不限数量，写出所有需要跨 task 保持一致的规则
- 如果判断任务之间完全独立无需协调，输出空对象
- **关键原则：宁可写多，不可遗漏**"""


PRINCIPLE_USER = """## 用户原始指令:
{user_message}

## 输出框架:
{framework}

## 任务列表:
{task_list}

请根据上述框架和任务，生成编写准则。只输出 JSON。"""


# ═══════════════════════════════════════════════════════════
# 阶段一 Part III: 任务定义审查
# ═══════════════════════════════════════════════════════════

PHASE1_TASKREVIEW_SYSTEM = """你是一个智能助手，正在以 Agent 模式工作。
当前阶段：审查任务定义的合理性。

你的任务：检查阶段一生成的每个 task 定义是否合理，确保后续填充能顺利进行。

## 审查要点：
1. **描述清晰度**：每个 task 的 description 是否足够具体，能让填充者独立完成任务？模糊的描述（如"处理数据""写相关内容"）必须修正
2. **task 间重叠**：是否有两个或多个 task 覆盖相同或高度相似的内容？重叠会导致重复输出，必须合并或划清边界
3. **文件分配**：每个 task 的 files 是否包含了完成该任务所需的全部相关文件？是否关联了不相关的文件？
4. **要求具体性**：requirements 是否列出了具体的验收标准和关键约束？
5. **task 粒度**：是否有 task 范围过大（应拆分）或过小（应合并）的情况？

## 输出规则：

### 所有 task 定义合理：
仅输出: PASS

### 需要修正：
输出 JSON 格式：
```json
{
    "passed": false,
    "feedback": "说明哪些 task 有什么问题",
    "corrected_tasks": {
        "task1": {
            "description": "修正后的描述（仅列出需要修正的 task，未修改的 task 也要包含以保持完整）",
            "files": ["修正后的文件列表"],
            "requirements": "修正后的要求",
            "preset": "修正后的预设（python/c/cpp/java/javascript/html/css/shell/batch/sql/yaml/json/markdown/plaintext/article/roleplay/dialogue/csv/structured/code/mixed）"
        }
    }
}
```

## 重要约束：
- 只修正有问题的 task 定义，不要改变框架结构
- 保持所有 task ID 不变
- corrected_tasks 必须包含所有 task（包括未修改的），确保完整性
- 不要因为小的措辞问题要求修正，只关注会导致填充失败的严重问题"""


PHASE1_TASKREVIEW_USER = """## 用户指令:
{user_message}

## 文件列表:
{file_list}

## 当前任务定义:
{task_list}

## 对话历史（简要）:
{history}

## 上次审查反馈:
{previous_feedback}

请审查以上任务定义。全部合理则输出 PASS，否则用 JSON 格式指出问题并给出修正。"""
